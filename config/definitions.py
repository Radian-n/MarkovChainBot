import os

# User setup:
FILE_NAME = "Radian_n"  # Also expected twitch username. This is used to parse/clean file in remove_name() function
CHANNEL_LIST = ["radian_n","akiwoo"]

# Don't touch
ROOT_DIR = os.path.realpath(os.path.join(os.path.dirname(__file__), ".."))
